/**
 * Day 1: planning only. Provider is sqlite for local-first dev.
 * Models will be added on Day 2 when we persist documents/chunks/embeddings.
 */
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// Models will be defined tomorrow:
// model Document { ... }
// model Chunk    { ... }
// model Embedding { ... }

